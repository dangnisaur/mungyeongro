rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 데이터 접근은 서버(firebase-admin)를 통해서만 한다.
    // 클라이언트 SDK의 직접 접근은 전면 차단.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
