# 멍경로 (MungGyeongRo) — PROJECT.md

> 반려동물 동반 문경 여행 코스 추천 서비스.
> 한국관광공사 TourAPI(반려동물 동반여행 정보)를 기반으로, 반려견 프로필에 맞춘
> 개인화 여행 코스를 생성하는 공모전 출품작.

> **주의**: 원본 기획 문서 중 1~12번 섹션은 세션에 전달되지 않아,
> 13~17번 섹션과 문맥을 바탕으로 아래 1~12번을 재구성했다. (결정 로그 #1 참고)

---

## 1. 서비스 개요

- **이름**: 멍경로 ("멍멍이 + 문경 + 경로")
- **한 줄 소개**: 우리 강아지 성향에 딱 맞는 문경 여행 코스를 AI가 짜주는 서비스
- **핵심 서사**: 거주자의 문제 인식 → 공공데이터 활용 → AI 개인화 → 지역(문경) 활력 기여
- **타깃**: 반려동물 동반 여행을 계획하는 수도권/인근 지역 보호자

## 2. 해결하는 4가지 공백

1. **정보 공백** — 반려동물 동반 가능 여부가 흩어져 있고 신뢰하기 어렵다
2. **개인화 공백** — 대형견/소형견, 활동량, 실내외 선호에 따라 갈 수 있는 곳이 다른데 일괄 추천뿐
3. **동선 공백** — 개별 장소 정보는 있어도 "하루 코스"로 엮어주는 서비스가 없다
4. **안전 공백** — 여행 중 응급상황 시 근처 동물병원 정보를 즉시 찾기 어렵다

## 3. 기술 스택

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) + React + TypeScript |
| 스타일 | Tailwind CSS v4 + shadcn/ui 스타일 컴포넌트 |
| DB | Supabase (PostgreSQL + PostGIS) + Prisma |
| 인증 | Supabase Auth (이메일 + 구글) |
| 지도 | 카카오맵 JS SDK |
| 공공데이터 | 한국관광공사 TourAPI 4.0 (KorPetTourService, KorService) |
| 테스트 | Vitest |
| 배포 | Vercel |

## 4. 데이터 소스

- **TourAPI 4.0 반려동물 동반여행 서비스(KorPetTourService)**: 동반 가능 시설, 동반 조건
- **TourAPI 4.0 국문 관광정보(KorService1)**: 지역기반 관광정보 (areaCode=35 경북, 문경시 시군구코드)
- 응급 동물병원: 공공데이터포털 동물병원 인허가 데이터 (데모는 수기 시드)
- API 키 없이도 돌아가도록 **데모 시드 JSON 폴백** 내장 (결정 로그 #2)

## 5. 데이터 모델 (요약)

- `Pet` — 반려견 프로필: 이름, 견종, 크기(소/중/대), 나이, 활동량(1~5), 사회성(1~5), 실내 선호
- `Place` — 시설: TourAPI contentId, 카테고리, 좌표, 반려동물 동반 정책, 태그[], 실내 여부, 평균 체류시간
- `Route` / `RouteStop` — 생성된 코스와 순서별 방문지
- `Visit` / `Feedback` — 방문 이력과 만족도(1~5) → 태그별 선호 가중치 재계산
- 상세는 `prisma/schema.prisma`

## 6. 추천 알고리즘 (src/lib/recommend/)

```
score(place, pet, ctx) =
    0.40 × tagAffinity(pet, place)      // 크기·활동량·실내외 적합도
  + 0.25 × themeMatch(ctx.theme, place) // 선택 테마와의 매칭
  + 0.20 × feedbackBoost(userWeights, place.tags) // 피드백 학습 보정
  + 0.15 × popularity(place)            // 기본 인지도/데이터 완성도
  − hardPenalty(pet, place)             // 대형견 불가 등 제약 위반 시 탈락
```

- 코스 구성: 시간 예산(반나절 4h / 하루 7h) 안에서 **탐욕적 최근접 이웃 + 카테고리 다양성 제약**
  (카페 연속 배치 금지, 점심 시간대에 식당 배치, 이동시간은 하버사인 거리 기반 추정)
- 피드백 루프: 만족도 4~5 → 해당 시설 태그 가중치 +, 1~2 → −, 지수이동평균으로 갱신

## 7. 페이지 구성

| 경로 | 내용 |
|---|---|
| `/` | 랜딩: 문경 스토리텔링, 통계, 4가지 공백, API 크레딧, 미니 지도 |
| `/plan` | 프로필 선택 + 테마/시간 → 코스 생성 |
| `/routes/[id]` | 코스 상세: 지도 + 타임라인 + 응급 동물병원 오버레이 |
| `/pets` | 반려동물 프로필 CRUD |
| `/history` | 방문 이력 + 만족도 피드백 |
| `/places` | 시설 브라우징 (보조) |

## 8. UI/UX 방향

- 톤: **친근하고 따뜻한** 컬러 (앰버/오렌지 계열 포인트, 크림 배경)
- 모바일 우선 반응형, 큰 터치 타깃
- 데이터가 비어 보이지 않게: 랜딩에도 지도/시설 수 노출
- 시설 카드에 반려동물 동반 조건을 항상 명시

## 9~12. (원본 문서 미전달 — 재구성 범위에 포함됨)

9~12번의 세부 내용(화면 설계, API 명세 등)은 위 5~8번에 통합 재구성했다.

---

## 13. 개발 우선순위 (4주 로드맵)

### 1주차 — 기반 구축
- [ ] Next.js + TS + Tailwind 초기화
- [ ] Supabase 프로젝트 생성, PostGIS 활성화
- [ ] Prisma 스키마 작성 및 마이그레이션
- [ ] shadcn/ui 셋업
- [ ] TourAPI 호출 라이브러리 (`src/lib/tourapi/`)
- [ ] `seed.ts`로 문경시 데이터 전체 적재
- [ ] tag-extractor 로직
- [ ] Supabase Auth (이메일 + 구글)

### 2주차 — 핵심 도메인
- [ ] 반려동물 프로필 CRUD (UI + API)
- [ ] 추천 알고리즘 코어 (`src/lib/recommend/`)
- [ ] Vitest 단위 테스트 (점수 계산 검증)
- [ ] `/plan` 페이지 (프로필 선택 + 테마/시간)

### 3주차 — 시각화
- [ ] 카카오맵 컴포넌트
- [ ] `/routes/[id]` 페이지 (지도 + 타임라인)
- [ ] 응급 동물병원 오버레이
- [ ] 모바일 반응형 마무리

### 4주차 — 학습 루프 & 배포
- [ ] 방문 이력 UI
- [ ] 만족도 피드백 → 점수 재계산 반영
- [ ] 데모용 시드 데이터 (사용자 2~3, 방문이력 10건)
- [ ] Vercel 배포
- [ ] README 정리
- [ ] 데모 영상 촬영 준비

## 14. 코딩 규칙

- 모든 파일 TypeScript, `any` 금지 (`unknown` → narrow)
- 컴포넌트는 함수형 + hooks
- 서버 컴포넌트 우선, 인터랙션 필요 시에만 `'use client'`
- 외부 API 호출은 반드시 `src/lib/` 안에 래핑 (UI에서 직접 호출 금지)
- 인증키/시크릿은 절대 클라이언트에 노출하지 말 것
- 커밋 메시지: Conventional Commits (`feat:`, `fix:`, `refactor:` 등)
- 커밋 자주 (기능 단위)
- 주요 결정 바뀌면 PROJECT.md 즉시 업데이트

## 15. 진행 방식 / 16. 평가 관점 / 17. 당부

(원본 문서 그대로 유지 — 요약: 자율 진행, 중요 결정은 결정 로그에, 막히면 블로커에 기록.
심사 기준: 공공데이터 활용도·완성도·창의성·지역 특화성·확장성.
완벽함보다 작동하는 MVP + 매력적인 데모.)

---

## 진행 현황

- [x] Next.js + TS + Tailwind 초기화 (2026-07-26)
- [ ] 이하 로드맵 진행하며 업데이트

## 결정 로그

1. **(2026-07-26) 원본 기획서 1~12번 재구성** — 세션에 13~17번만 전달됨.
   서비스 개요/스택/데이터 모델/알고리즘을 문맥 기반으로 재구성. 원본과 다른 부분이 있으면
   발견 즉시 이 문서를 수정할 것.
2. **(2026-07-26) 데모 모드 아키텍처** — TourAPI 키·Supabase·카카오맵 키가 아직 없으므로,
   데이터 접근을 repository 레이어로 추상화하고 환경변수 부재 시 `src/data/demo/*.json`
   폴백으로 전 기능이 로컬에서 동작하게 함. 키가 준비되면 env만 채우면 실데이터로 전환.
   **데모 시드 데이터는 실제 검증된 정보가 아니므로 제출 전 반드시 TourAPI 실데이터로 교체할 것.**
3. **(2026-07-26) 인증 지연 결합** — Supabase 프로젝트가 없어 Auth는 어댑터만 구현하고
   데모 모드에서는 로컬 데모 사용자로 동작. (블로커 #1)
4. **(2026-07-26) shadcn/ui는 CLI 대신 필요한 컴포넌트만 수기 포팅** — 프로젝트 경로가
   한글이고 오프라인 안정성을 위해 button/card/input 등 필수 컴포넌트만 동일 스타일로 직접 작성.

## 블로커

1. **Supabase 프로젝트 미생성** — URL/anon key 필요. 데모 모드로 우회 중.
2. **TourAPI 인증키 미발급** — 공공데이터포털에서 KorPetTourService 활용신청 필요. 데모 시드로 우회 중.
3. **카카오맵 JS 키 미발급** — developers.kakao.com에서 앱 생성 필요. 키 없으면 정적 폴백 지도 렌더.
